# 第一阶段更正验证报告

## 2026-09-21 课表编辑、拖拽与作息

| 验证项 | 结果 |
|---|---|
| 前端 Vitest | 30/30 通过 |
| Rust 存储层 | 12/12 通过 |
| TypeScript 与 Vite 生产构建 | 通过 |
| Tauri 鼠标拖放人工验收 | 未执行 |

- 覆盖 AI 作息 JSON 解析、重复节次/时间顺序校验，以及“某几周”输入解析。
- 课程、课表与作息均由 Rust 端再次校验并写入本地 Markdown 数据目录。

验证日期：2026-09-12

## 自动化结果

| 验证项 | 结果 |
|---|---|
| 前端领域规则测试 | 28/28 通过 |
| Rust Markdown、课表与雅思存储测试 | 9/9 通过 |
| TypeScript 严格检查 | 通过 |
| Vite 生产构建 | 通过 |
| Rust/Tauri 测试编译 | 通过 |

覆盖内容包括：

- 点击课程创建任务时自动关联课程、类型默认“作业”、开始时间取创建时刻。
- 课程任务的子类型字段、可选自定义类型及后端合法性校验。
- 全部任务按雅思、竞赛、课程、其他分组；雅思和竞赛不提供快捷完成入口。
- 日历同时按教学周范围和星期判断是否需要课程日背景。
- AI 返回“说明文字 + Markdown JSON 代码块”时仍能提取并导入课表。
- 已核对本机应用数据：用户提供的课表已成功生成 11 条课程记录及 `SCHEDULE.md` 索引。
- 雅思每科最近 5 条已完成记录平均分、最近模考、完成时间倒序及科目筛选。
- 阅读必须保存 3 篇答对数，听力必须保存 4 个部分答对数；分数限制为 0–9。
- 开学日期默认当年 9 月 1 日并可持久保存，任意日期都能据此换算教学周；非有效周次不再显示课程或标黄。
- 同一课表格中的分段周次记录会合并，不同课程并排布局，避免文字叠印。
- 雅思课程 JSON 导入、上课时间排序以及四科总分取最近 0.5 分档规则。
- 雅思课程兼容 `date + startTime + endTime` 及 `&#x20;` 空格实体，并转换为统一时间记录。
- 雅思课程按具体日期进入日历，使用淡紫色标记，与普通课程的荧光黄区分。
- 阅读支持完成 1–3 篇，每篇答对数最多 15；听力支持完成 1–4 部分，每部分最多 10。
- 阅读或听力部分完成时不保存虚假的分数，只汇总显示答对题数与总题数。
- 阅读每篇、听力每部分均可单独设置总题数，答对数不能超过相应总题数。
- 已完成雅思作业支持修改、确认后软删除、垃圾箱查看与恢复；垃圾箱记录不参与成绩统计。
- 日历雅思课程标记显示当天具体课程名称，并自动合并重复名称。
- 阅读与听力完成全部篇/部分时，按实际答对数与总题数折算至 40 题制后自动匹配分数；前后端使用相同规则，部分完成仍不生成分数。
- 雅思作业卡片采用时间双环：外环按实际/预计时间显示进度（超时封顶一圈），预计与实际用时均置于圆内；阅读与听力题数摘要使用紧凑字号。

## 界面与结构检查

- 浏览器预览确认雅思区域显示左侧四入口：目标、作业与自测、课程、历史模考成绩。
- “作业与自测”同页显示未完成与已完成，具备全部/阅读/听力/写作/口语筛选及添加作业入口。
- 完成圆环内显示分数和实际用时，圆环外显示截止时间和应使用时间；四个科目使用不同颜色。
- 课程页具备“本周 / 全部课程”切换，点击课程进入课程专属新增任务表单。
- 课程专属表单不显示优先级，并把任务描述与备注合并成一个输入项。
- 日历课程日使用荧光黄背景；全部任务按类型分区显示。
- “本周课程”和“全部课程”使用相同周课表格式；本周只显示当前教学周有效的课程。
- 开学日期设置位于“本周课程”，本周内容直接从全部课程按周数筛选，无需重复导入。
- 雅思课程改为 AI 模板导入，并按日期及上课时间纵向排列。
- 雅思目标页在四科成绩之外显示自动计算的总分。
- 雅思新增与完成弹窗移除全屏黑色伪遮罩，保持与课程区域一致的浅色界面。

浏览器预览不能调用 Tauri 本地接口，因此预览中的 `invoke` 提示不作为桌面端故障；Rust/Tauri 接口已通过编译与存储层测试。开发窗口已在本次修改后完成热重载和后端重启，但 Windows 控制接口未返回可连接的原生应用窗口，因此完整人工点击流程仍需按 `TEST_PLAN.md` 执行，不把它标记为已通过。

Rust 自动格式化组件在本机工具链中未安装；这不影响本次编译和测试结果。

## Windows 桌面成品

- 2026-09-13 完成 Tauri Release 编译，生成免安装桌面程序 `桌面版/近期规划.exe`。
- 已实际启动 Release 成品；进程名与窗口标题均为“近期规划”，Windows `Responding` 状态为 `True`。
- MSI 打包所需的 WiX 工具因下载超时未生成；免安装版不依赖安装器，可直接双击运行。
