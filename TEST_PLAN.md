# 第一阶段验证与测试方案

## 目标闭环

本阶段先验证以下核心流程：

1. 用户可在首页 3 步内创建任务。
2. 新任务立即同时出现在首页、任务页和日历页。
3. 用户可编辑任务、标记完成或未完成、取消任务，并看到一致状态。
4. 删除必须二次确认；确认后执行软删除，取消后数据不变。
5. 关闭并重新打开应用后，任务从 Markdown 文件恢复。
6. 每次写入后 `INDEX.md` 自动重建；索引丢失后应用启动可重建。
7. 无网络时以上功能不受影响。
8. 课表拥有独立导入入口，可导入 AI 按模板返回的 JSON 文件或文本。
9. 导入后课程显示在周课表；重复导入同一课程时合并而非重复创建。
10. 从周课表点击课程可新增作业，并自动关联该课程。
11. 新建“课程”类型作业时必须选择一个已导入课程。
12. 点击课表课程后显示课程专属任务表单，默认开始时间为创建时刻，且不显示优先级。
13. 全部任务按顶层类型分组；雅思和竞赛任务不显示快捷完成按钮。
14. 日历中存在导入课程的星期使用荧光黄背景。
15. 雅思最近作业平均值仅取每科最近 5 条已完成记录，不足 5 条取全部。
16. 雅思历史模考按时间倒序展示，最近一次模考正确进入目标摘要。
17. 阅读完成记录包含 3 篇答对数；听力完成记录包含 4 个部分答对数。
18. 雅思“作业与自测”可按四科筛选，且未完成、已完成在同一页面分区显示。
19. 雅思专属区域提供目标、作业与自测、课程、历史模考成绩四个入口。
20. 选择开学日期后，可正确计算任意日期的教学周；课表“本周”和日历标黄均受周次范围约束。
21. 同一星期、同一节次的分段周次记录合并展示，不发生文字叠印。
22. 雅思课程可按 AI JSON 模板导入，并按上课时间纵向排序。
23. 雅思目标、近期作业平均和最近模考均显示按四科计算的总分。
24. 未设置开学日期时默认使用当年 9 月 1 日；本周课程直接从全部课程按教学周筛选。
25. 雅思课程兼容 `date + startTime + endTime` 格式及 AI 回复中的 `&#x20;` 空格实体。
26. 雅思课程按具体日期进入日历，并使用区别于普通课表的颜色。
27. 阅读允许只完成 1–2 篇，单篇答对数不超过 15；部分完成不生成分数，只显示总题数与答对数。
28. 听力允许只完成 1–3 个部分，每部分答对数不超过 10；部分完成不生成分数，只显示总题数与答对数。
29. 阅读每篇和听力每部分均可填写各自总题数，答对数不能超过对应总题数。
30. 已完成雅思作业可修改结果、软删除，并能从垃圾箱恢复。
31. 日历中的雅思课程标记显示当天具体课程名称，而不是统一的“雅思课”。
32. 阅读和听力完成全部篇/部分后，按答对数与实际总题数折算到 40 题制并自动匹配雅思分数；部分完成仍不生成分数。
33. 雅思作业卡片的时间双环按“实际用时 / 应使用时间”显示进度，超时封顶为完整一圈，两项时间均显示在圆环内部。
34. 点击课程可修改名称、地点、老师、星期、节次和时间；保存后刷新或重启仍一致。
35. 将课程拖到另一格后，必须先选择“全部周次”或输入若干周次；只改部分周时，未选周保持原安排。
36. 按住课程左上或右下圆点拖动可分别调整开始或结束节次，且结束节次永远不早于开始节次。
37. 课表名称、开学日期与颜色可编辑且持久化；课表作息默认为空。
38. 可粘贴 AI 作息 JSON；保存后每节课的起止时间显示在课表左侧，非法时间或重复节次被拒绝。

## 自动化验证

### Rust 存储层

- Markdown 写入并重新解析后，核心字段完全一致。
- 中文、标签、空可选字段和包含特殊字符的正文可往返保存。
- 非法标题、类型、优先级、状态和记录 ID 被拒绝。
- 内容哈希在内容不变时稳定，内容变化时更新。
- 写入采用临时文件及替换策略，不留下半成品记录。
- 软删除记录不再进入活动索引，但原 Markdown 文件仍保留。

运行：`cargo test --manifest-path src-tauri/Cargo.toml`

### React 领域与界面层

- 自动判断今日、未来 7 天和逾期任务。
- 已完成、已取消任务不计入逾期。
- 搜索、类型筛选和状态筛选结果正确。
- 月历能把任务映射到正确日期。
- 新增表单阻止空标题，并正确提交标准化数据。
- 完成任务后首页、任务页和日历使用同一状态源刷新。
- 课表 JSON 模板的必填字段、星期、节次和周次范围校验正确。
- 课表中的课程可按星期和节次映射到周课表。
- 全部任务按顶层任务类型稳定分组。
- 日历课程日标记规则正确。
- 雅思最近 5 次平均分、最近模考和完成时间排序正确。
- 雅思作业的全部/单科筛选结果正确。
- AI 作息 JSON、指定周次字符串和课程单元格映射校验正确。

运行：`pnpm test`

### 静态与编译验证

- TypeScript 严格检查和 Vite 生产构建通过：`pnpm run build`。
- Rust/Tauri 桌面核心编译通过：`cargo check --manifest-path src-tauri/Cargo.toml`。

## 手动桌面验收

使用 `pnpm run tauri dev` 启动后执行：

1. 从首页点击“新增任务”，填写名称和截止时间，保存。
2. 分别打开首页、全部任务、日历，确认同一任务及时间一致。
3. 编辑名称和优先级，确认三个页面同步更新。
4. 标记完成，再改为未完成，确认状态即时刷新。
5. 点击删除后先取消，确认任务保留；再次删除并确认，确认任务消失。
6. 新建一条任务，完全退出应用并重新打开，确认数据恢复。
7. 检查应用数据目录：记录位于 `data/records/tasks/<record_id>.md`，并存在可读的 `data/INDEX.md`。
8. 断开网络后重复新增、编辑、完成和重启恢复流程。
9. 在“本周课程”中修改开学日期并保存，重新打开应用后确认日期仍存在；未设置时默认当年 9 月 1 日。
10. 对照当前教学周检查“本周课程”，再切换“全部课程”；确认两者布局一致且日历只在有效周次标黄。
11. 在雅思课程页复制 AI 模板，导入两条不同时间的课程，确认按时间从上到下排列。
12. 在雅思目标页核对目标、最近模考、近期作业平均三处的总分计算。
13. 点击一门课程，修改名称、老师和地点后保存，重启应用确认内容保持；再拖至另一星期/节次，确认弹窗选择“全部周次”后保存。
14. 对一门跨多周课程拖动时间后选择“只改某几周”，填写 `1,3-5`，确认选中周移至新位置、其余周不变；拖动左上与右下圆点确认可缩短/延长节次。
15. 点击“编辑课表”修改名称；点击“作息（未设置）”，粘贴 AI 作息模板并保存，确认左侧对应节次显示时间；输入结束早于开始的 JSON，确认被拒绝。
16. 未进入“修改课表”时点击课程，确认直接打开该课程的新增作业表单；进入修改课表后，确认点击课程打开课程编辑而非新增作业；点击“完成修改”后退出该模式。
17. 进入修改课表后按住课程块并移动，确认其在网格中实时预览新的星期/节次；按住左上或右下圆点并移动，确认课程块高度实时变化；两种操作均在松开鼠标后才出现周次范围选择。
18. 确认“修改课表”入口位于周课表与课程作业之间右侧；调整任一课程后点击“取消并恢复”，确认页面回到原位置且重启后数据未改变；再次调整并点击“保存修改”，确认数据持久化且退出修改模式。

## 通过标准

- 所有自动化命令退出码为 0。
- 手动验收 8 项全部通过。
- 若受运行环境限制无法完成某项，必须明确标记“未验证”，不得视作通过。
